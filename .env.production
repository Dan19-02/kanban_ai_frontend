# Production build config (committed — contains NO secrets).
# Vite loads this only for `vite build` (production mode), so local `npm run dev`
# still proxies to a local backend. Change this if the backend URL changes,
# then rebuild/redeploy the static site.
VITE_API_URL="https://kanban-ai-akx1.onrender.com"
