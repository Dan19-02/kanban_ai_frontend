# Production build config (committed — contains NO secrets).
#
# The app authenticates with a Bearer token (Authorization header), so it works
# cross-origin on every device (including mobile, where third-party cookies are
# blocked). Point VITE_API_URL directly at the deployed backend — no proxy needed.
#
# If you also set VITE_API_URL in the Vercel dashboard it overrides this file;
# keep them the same.
VITE_API_URL="https://kanban-ai-akx1.onrender.com"
